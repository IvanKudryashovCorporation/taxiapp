# Backend URL — proxy в next.config.mjs использует эту переменную.
# Скопируйте в .env.local и при необходимости поменяйте.
NEXT_PUBLIC_BACKEND_URL=http://127.0.0.1:8000

# Токен оператора (для тестирования без auth-flow)
NEXT_PUBLIC_OPERATOR_TOKEN=
